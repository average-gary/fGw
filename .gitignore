node_modules/
dist/
.DS_Store
*.log
.env
.env.local
.env.*.local
src-tauri/target/
src-tauri/gen/
src-tauri/Cargo.lock
target/
.vite/
.vscode/
.idea/
coverage/
playwright-report/
test-results/
plan.md
.test-key.json
public/guides/figures/.discarded/

# FGW source-manual PDFs — reproducible from .wiki/raw/papers/_pdfs-manifest.md
# (URLs + SHA256s captured there). Markdown extracts under extracts/ are kept;
# the binary blobs are not.
.wiki/raw/papers/pdfs/

# Android release-signing secrets — never commit
# (already covered by `src-tauri/gen/` above; explicit line is defense in depth
# in case that broad rule is ever loosened. The template lives at
# `src-tauri/keystore.properties.template`, outside the ignored directory.)
src-tauri/gen/android/keystore.properties

