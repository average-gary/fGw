{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities — refined per SPEC as features land.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "sql:default",
    "stronghold:default",
    "barcode-scanner:default"
  ]
}
